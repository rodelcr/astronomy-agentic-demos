"""transit.py — measure a planet's radius ratio from a transit light curve.

STUB (step 1). The real implementation arrives at step 5.
"""
from __future__ import annotations


def rp_over_rstar(depth):
    """Radius ratio from fractional transit depth (uniform source). (stub)"""
    raise NotImplementedError("implemented at step 5")


def fit_transit(time, flux, flux_err, period, t0):
    """Phase-fold and fit a transit; return the radius ratio Rp/R*. (stub)"""
    raise NotImplementedError("implemented at step 5")
