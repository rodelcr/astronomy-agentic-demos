# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.pytest_cache/
*.egg-info/

# Env
.venv/
env/

# OS
.DS_Store

# Large transient data (regenerated from seed / fetch scripts — never committed)
# The Nside-2048 CMB maps are ~400 MB; only the small bandpowers CSV is committed.
07_cmb_power_spectrum/project/data/real/_smica_full.fits
*.fits.tmp
# Ephemeris download temp files (the committed de440s.bsp itself is kept).
08_solar_system/project/data/kernels/_*.bsp
*.bsp.tmp

# NOTE: we deliberately DO commit results/*.png and bundled data/ —
# the figures are meant to be inspected on GitHub and the data keeps
# every demo runnable offline.
